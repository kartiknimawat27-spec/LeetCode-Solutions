class Solution:
    def maxSubArray(self, nums):
        sum = 0
        maxi = nums[0]

        for num in nums:
            sum += num

            if sum > maxi:
                maxi = sum

            if sum < 0:
                sum = 0

        return maxi